@ricky0123/vad-web - gives you a "Speech Started" and "Speech Ended" event. ==
For Recording: Use the browser's native MediaRecorder API. ==
Framer Motion v10+ == animation library ==
Lucide React (icons)
Zustand == client side state managmnent ==
TanStack Query == server side state managment
-----------------------
- implement language switching
- set up convex and clerk and have the user account flow buildt 
- build dark/light mode switching
- add terms by seeing how it was done with remove.bg
- build price page and integrate paddle payment using thier sandbox
- build the logic to calculate the hours and tracking it for each user
- make the 'recording' stops in another way which is by pressing or tapping the screen
- display the transcript of both languages in each loop
- Mobile "Wake Lock": Since this is a web app, mobile browsers love to kill the microphone or dim the screen if there's no "activity.
    " You'll need to implement the Screen Wake Lock API so the phone doesn't go to sleep in the middle of a long conversation.
